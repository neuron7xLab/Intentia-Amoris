# Risk Lattice

R1 state: Evidence -> api read compute write. Mechanism -> stale row write. Blast Radius -> state drift. Scale Trigger -> same pair burst. Probe -> parallel event test. Minimal Fix -> state service.

R2 retrieval: Evidence -> all chunks sorted in Python. Mechanism -> linear scan. Blast Radius -> latency wall. Scale Trigger -> 100k chunks. Probe -> fixture benchmark. Minimal Fix -> vector SQL